import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [react()],
  server: {
    host: true
  },
  // 确保构建时能正确处理根目录下的 App.jsx
  build: {
    outDir: 'dist',
  }
});
